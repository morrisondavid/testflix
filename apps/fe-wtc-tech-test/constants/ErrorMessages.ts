const ErrorMessages = {
  REDUCER_ACTION_HANDLERS_MISSING:
    'Cannot create reducer. No action handlers could be found',
  REQUIRED_PARAMETER: 'The parameter is required',
  INVALID_RATING: 'Invalid movie rating',
};

export default Object.freeze(ErrorMessages);
